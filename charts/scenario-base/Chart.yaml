apiVersion: v2
name: scenario-base
description: >
  Generic chart for a sandbox scenario's mock data-center topology — renders
  one Deployment + Service per component listed under .Values.components.
  See docs/Sandbox_Architecture.md section 3.1 for how a scenario config
  points at this chart, and services/sandbox_service/k8s_runner.py for how
  components gets populated per session via --set-json.
type: application
version: 0.1.0
appVersion: "0.1.0"
