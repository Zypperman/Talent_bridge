# Overridden per-session via `helm upgrade --install <session-id> charts/scenario-base
# --set-json components=<json>` — see services/sandbox_service/k8s_runner.py
# install_topology(), which passes the scenario's topology.components list
# straight through from its YAML config.
components: []
# Example shape:
# components:
#   - name: storage-array
#     image: mock-iscsi-target:latest
#     port: 3260
#   - name: app-server
#     image: mock-app-server:latest
#     port: 8080
