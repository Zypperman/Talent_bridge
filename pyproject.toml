[project]
name = "talent-bridge"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "bcrypt",
    "openai",
    "python-dotenv",
    "libsql",
]

[tool.vercel]
entrypoint = "gateway.main:app"
